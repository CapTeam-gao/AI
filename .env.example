# 로컬에서 AI 서버를 직접 실행할 때는 http://localhost:8080을 사용하세요.
# AI 서버를 Docker로 실행하고 호스트의 백엔드를 호출할 때는
# http://host.docker.internal:8080을 사용하세요.
BACKEND_BASE_URL=http://localhost:8080

# Backend/.env의 INTERNAL_MATCHING_API_KEY와 반드시 같은 값을 사용하세요.
INTERNAL_MATCHING_API_KEY=local-dev-test-key-not-for-prod
